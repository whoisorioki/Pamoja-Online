SUPABASE_URL=
SUPABASE_ANON_KEY=
# Current rotated space passphrases — used by the live RLS integration tests
# (tests/integration/rls-live.test.js) when `npm test` runs against the live
# Supabase database. Leave unset to fall back to the local dev seed values.
MENS_SPACE_PASSPHRASE=
WOMENS_SPACE_PASSPHRASE=
